<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atinbo.generate.mapper.GenerateMapper">

    <resultMap type="com.atinbo.generate.model.TableInfo" id="TableResult">
        <result property="tableName" column="TABLE_NAME"/>
        <result property="tableComment" column="TABLE_COMMENT"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="updateTime" column="UPDATE_TIME"/>
    </resultMap>

    <resultMap type="com.atinbo.generate.model.ColumnInfo" id="ColumnResult">
        <result property="columnName" column="COLUMN_NAME"/>
        <result property="columnComment" column="COLUMN_COMMENT"/>
        <result property="dataType" column="DATA_TYPE"/>
        <result property="columnKey" column="COLUMN_KEY"/>
    </resultMap>

    <select id="selectTableList" resultMap="TableResult">
		select TABLE_NAME, TABLE_COMMENT, CREATE_TIME, UPDATE_TIME from information_schema.TABLES
		where TABLE_SCHEMA = (select database())
	</select>

    <select id="selectTableInfo" resultMap="TableResult">
		select TABLE_NAME, TABLE_COMMENT, CREATE_TIME, UPDATE_TIME from information_schema.TABLES
		where TABLE_SCHEMA = (select database())
        <if test="tableName != null and tableName != ''">
            AND TABLE_NAME = #{tableName}
        </if>
	</select>

    <select id="selectColumnList" resultMap="ColumnResult" parameterType="string">
        select COLUMN_NAME, COLUMN_COMMENT, DATA_TYPE, COLUMN_KEY from information_schema.COLUMNS
        where TABLE_SCHEMA = (select database())
        <if test="tableName != null and tableName != ''">
            AND TABLE_NAME = #{tableName}
        </if>
    </select>
</mapper>
